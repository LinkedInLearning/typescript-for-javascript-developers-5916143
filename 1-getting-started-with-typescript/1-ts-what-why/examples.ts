// This is how to write it in JavaScript

let name1 = "Zia";

function add1(x, y) {
  return x + y;
}

 
// This is how to write it in TypeScript

let name2: string = "Zia";

function add2(x: number, y: number): number {
  return x + y;
}
